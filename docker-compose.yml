services:
  # run jupyter notebook inside jupyter 
  jupyter-notebook:
    image:  bfrizzell/mushroom_classifier:f1d0d77
    ports:
      - "8888:8888"
    volumes:
      - .:/home/jovyan
    deploy:
      resources:
        limits:
          memory: 5G
    platform: linux/amd64
